---js
// <script>
const pagination = {
	data: "collections",
	size: 1,
	alias: "tag",
	filter: ["all", "posts"],
	// addAllPagesToCollections: true,
};

const layout = "layouts/page";

const eleventyExcludeFromCollections = true;

const eleventyComputed = {
	title: 'Posts tagged “{{ tag }}”',
	permalink: function(data) {
		return `/${this.slugify(data.tag)}/`;
	},
  subhead: function(data) {
    return `Titles only &middot; <a href="/${this.slugify(data.tag)}/full/">Full posts</a><br><span class="arrow">←</span> <a href="/tags/">Index</a>`;
  }
};
---

{% set postslist = collections[ tag ] %}
{% include "components/posts-list.njk" %}
